services:
  - type: web
    name: fr
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python SERVER.PY
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0